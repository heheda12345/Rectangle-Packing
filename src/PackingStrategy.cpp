#include "PackingStrategy.h"

namespace RECTPACKING {
/*
PackingStrategy::PackingStrategy(const PackingStrategy& packingStrategy)
{
    if (packingStrategy.m_pCommand)
        m_pCommand=new PackingCommand(packingStrategy.m_pCommand);
    else
        m_pCommand=nullptr;
}

PackingStrategy& operator = (const PackingStrategy& packingStrategy)
{
    if (&packingStrategy!=this)
    {
        if (packingStrategy.m_pCommand!=nullptr)
            m_pCommand=new PackingCommand(packingStrategy.m_pCommand);
        else
            m_pCommand=nullptr;
    }
    return *this;
}
*/
}
